# BVideo Studio 视觉开发约束

本文档是 `AGENTS.md` 的视觉专项补充。凡修改 `src/components/`、`src/styles/`，或任何会影响界面结构、视觉表现、主题、图标、动效和交互状态的代码，都必须同时遵守本文档与根目录 `AGENTS.md`。

## 1. 产品视觉定位

- BVideo Studio 是面向持续编辑工作的桌面视频工具，界面应专业、克制、紧凑、可扫描，优先保障效率和信息层级。
- 延续当前三栏工作区、中央画布和底部时间线的编辑器结构。不要把核心工作流改造成落地页、内容门户或装饰性卡片墙。
- 不使用超大标题、渐变背景、装饰光球、玻璃拟态、无意义插画或大面积留白制造“高级感”。视觉元素必须服务于内容识别或操作反馈。
- 不做单一色相铺满界面。中性表面承担结构，蓝色用于主要操作，绿色、橙色、红色和 AI 紫分别沿用已有语义。
- 用户可见文案使用简体中文，措辞短、直接、面向动作；不要在界面中加入讲解设计、介绍功能或说明快捷键的大段文案。

## 2. 信息架构与布局

- 高频编辑动作必须留在主工作区可直接访问的位置；设置、确认和低频管理任务放入对话框或既有侧栏，不新增无必要的页面跳转。
- 保持工具区、资源区、检查器、画布和时间线的职责清晰。不要把同一控制重复放进多个区域，也不要在一个面板中混入无关工作流。
- 不在卡片内继续嵌套卡片。页面分区使用边线、背景层级、间距和标题组织；卡片只用于独立的重复条目、确认提示或真正需要边界的工具。
- 固定格式区域必须有稳定尺寸：工具栏、图标按钮、轨道、时间尺、画布和输入行不能因 hover、加载文案、数值变化或图标切换发生跳动。
- Grid/Flex 子项需要按实际情况设置 `min-width: 0`、`min-height: 0` 和 `minmax(0, 1fr)`，避免长文件名、工程名或动态状态撑破布局。
- 长文本必须明确选择换行、滚动或省略策略。文件名、路径、模型名和工程名通常单行省略；错误说明和正文允许换行，不能遮挡后续操作。
- 不随意改变 `.app-shell` 的最小宽度、三栏比例和时间线高度。任务确需改变整体布局时，必须同时验证资源面板、画布、检查器和时间线仍可操作。
- 窗口高度较小时保证主要操作和弹窗底部按钮可达；弹窗使用受约束的最大高度与内部滚动，不允许内容溢出视口。

## 3. 尺寸、间距与层级

- 沿用当前紧凑尺度：正文约 13px、次要信息约 9-11px、面板内标题约 12-18px。不要在紧凑面板中使用 hero 级字号。
- 字号不得随视口宽度缩放；`letter-spacing` 保持为 `0`，不使用负字距压缩文本。
- 常规按钮和输入沿用约 30-34px 高度，纯图标按钮沿用约 24-30px 方形尺寸。相同层级的控件应保持一致高度和点击区域。
- 圆角保持克制：普通控件约 3-6px，对话框最多沿用现有 8px。不要新增胶囊形文本容器，除非它明确表示状态、数量或筛选条件。
- 间距优先复用相邻组件的 4/5/6/8/10/12/16/24px 节奏，不随意引入无法形成规律的数值。
- 层级优先通过背景表面、1px 边线、文字颜色和间距表达；阴影主要用于悬浮对话框，不给普通页面分区添加浮层阴影。

## 4. 颜色与主题

- 优先使用 `src/styles/main.css` 已定义的变量：`--bg`、`--surface-*`、`--line*`、`--text`、`--soft`、`--muted`、`--accent*` 及状态色。
- 不直接复制已有颜色值创建近似色。确需增加全局语义颜色时，在 `:root` 中命名，并同步定义浅色主题下的表现。
- 每个视觉变更都必须检查深色与浅色主题。文本、边线、禁用状态、选中状态和浮层不能只在默认深色主题下可读。
- 颜色不能是状态的唯一线索。选中、警告、错误、锁定、静音和隐藏状态还应通过图标、边框、文案、透明度或控件状态表达。
- 正文和关键控件需保持清晰对比度；`--muted` 仅用于次要信息，不能用于关键操作、表单值或错误说明。
- 素材类型和轨道颜色沿用现有语义，不因单个功能重新分配颜色，避免用户在资源区、时间线和检查器之间产生认知冲突。

## 5. 组件与控件选择

- 清晰命令使用文本按钮或图标加文本按钮；常见工具操作优先使用纯图标按钮；模式切换使用 Tabs 或分段控件；布尔设置使用 checkbox/toggle；数值使用输入框、滑块或步进器；有限选项使用 Select/Menu。
- 图标优先使用已安装的 `lucide-react`，不手绘 SVG，不使用字符模拟图标。图标大小和线宽应与同一工具栏的现有图标一致。
- 纯图标按钮必须提供中文 `aria-label`；需要补充含义时使用 `title` 或既有 tooltip 模式，不能依赖图标让用户猜测。
- 对话框、Tabs、Select 等复杂交互优先使用已安装的 Radix UI。新增弹窗必须具备标题、描述、关闭方式、焦点管理和明确的主次操作。
- 危险操作使用红色语义和确认步骤；取消/稍后使用次要按钮；一个操作区原则上只保留一个主要按钮。
- 表单控件必须有关联 label，单位紧邻数值。占位符用于示例或补充，不替代 label；校验错误靠近相关字段并可被辅助技术识别。
- 所有 `<button>` 显式设置 `type`。禁用控件必须有可辨识的禁用外观，并避免仍能通过旁路事件触发。

## 6. 状态与交互反馈

- 新增异步界面时设计完整状态：初始、加载、成功、空数据、失败、禁用和取消。状态变化不能导致主要控件位置大幅跳动。
- 加载中的按钮保留原有尺寸，使用 Lucide 加载图标或短文案；防止重复提交，并在失败后恢复可操作状态。
- hover、active、selected、focus-visible 和 disabled 必须有明确且一致的反馈。触屏或键盘用户不能依赖 hover 才能发现核心能力。
- 拖拽、裁剪、画布缩放和时间线操作应保持光标、选中态和预览反馈同步；拖动期间不得触发布局重排。
- 动画只用于状态过渡和空间关系，时长保持短促。遵守现有 `prefers-reduced-motion` 规则，禁止持续、抢眼或阻碍编辑的装饰动画。
- 成功反馈不应阻断连续编辑；错误反馈必须说明发生了什么以及用户下一步可以做什么，不显示密钥或冗长底层堆栈。

## 7. 画布、媒体与时间线

- 画布是内容检查区域，不作为装饰容器。必须完整显示实际素材、生成场景和叠加层，并保持项目画布宽高比。
- 画布内可操作元素的选择框、控制柄和层级不得改变导出内容；预览辅助 UI 与最终画面必须在数据和 DOM/CSS 上保持可区分。
- 新增视觉变换时检查横屏、竖屏和方形画布；基于画布百分比或统一变换模型计算，避免按当前 DOM 像素写死。
- 时间线轨道、片段、播放头、选区和缩放控件必须保持稳定对齐。片段内容过长时省略，不得改变轨道高度。
- 素材缩略图应展示真实内容并保持固定比例；加载失败、素材丢失和无缩略图时使用明确占位状态，不显示破损图片。
- 影响画布预览的视觉功能必须同步检查渲染计划和 FFmpeg 导出结果，不能用仅预览可见的 CSS 假装实现最终效果。

## 8. 可访问性

- 优先使用语义化 HTML。交互元素使用 `button`、`input`、`select` 等原生元素，不用带点击事件的 `div` 模拟控件。
- 保留全局 `:focus-visible`，不得仅为视觉整洁移除 outline。复合控件需要合理的键盘顺序和 Escape/方向键行为。
- 图标按钮必须有可访问名称；装饰图标使用 `aria-hidden`；有信息意义的图片提供合适 alt，纯装饰或已由相邻文字描述的缩略图可使用空 alt。
- 错误和阻断状态使用 `role="alert"` 或等价可访问机制；进度信息在需要时提供文字，不只显示动态图形。
- 点击目标应足够稳定且不被相邻元素遮挡。调整紧凑布局时，不能以牺牲准确点击、键盘操作或文字可读性为代价。

## 9. CSS 实现规则

- 视觉样式集中沿用 `src/styles/main.css` 的现有组织方式；只为有明确复用价值或维护边界的场景拆分新样式文件。
- 类名表达组件或语义状态，避免依赖脆弱的 DOM 层级和位置编号。已有组件类可扩展时，不创建含义相同的新类。
- 避免 `!important`；只有覆盖不可控第三方内联优先级且没有更合适方案时才允许，并说明原因。
- 不使用随内容变化的魔法偏移修补重叠。通过 Grid/Flex、稳定轨道、padding、gap、aspect-ratio 和 overflow 解决布局问题。
- z-index 只在明确的层级系统中使用。普通内容、工具栏、拖拽层、遮罩和对话框之间保持可解释顺序，不用不断增大的随机值修复遮挡。
- 媒体查询必须对应真实布局断点或高度限制；不要为单个设备型号写规则。新增规则需确认不会破坏现有 `max-width`、`max-height` 和 reduced-motion 行为。

## 10. 视觉变更验收

- 修改前确认相邻界面的组件、尺寸、颜色和交互模式，视觉实现应像现有产品的自然延伸。
- 完成后至少检查默认桌面尺寸、当前最小支持宽度、较矮窗口、深色主题、浅色主题以及受影响弹窗的最大内容状态。
- 检查所有动态文案：最长文件名/模型名、错误信息、空状态、加载状态、禁用状态和数值极值，确保无重叠、截断误导或布局跳动。
- 涉及画布时检查 16:9、9:16 和 1:1；涉及时间线时检查空工程、长工程、多轨道、缩放两端和选区状态。
- 组件行为测试使用 Testing Library，以 role、label 和可见文案查询。视觉布局变更在可运行环境中进行浏览器或桌面截图检查，并确认控制台无新增错误。
- 若环境限制导致无法完成截图、主题或桌面端验证，交付时明确说明未验证的视口、主题或运行时。
