# 2026-09-10

## 只读分析：edge 服务新增桌面更新后端支持

**结论：客户端更新字段契约（权威来源 tauri-plugin-updater 2.10.1 src/updater.rs）**

- 请求：`GET /api/desktop-updates/latest?platform=<target>`；target ∈ windows-x86 / windows-arm / macos-x86 / macos-arm / linux-x86。
- 版本比较在 Rust 侧完成（`release.version > current_version`），服务端只需返回「最新一条」，无需接收 current_version。
- 响应被反序列化为 `RemoteRelease`（`#[serde(untagged)]`）。本项目的端点模板只有 `{{target}}`，应使用 **Dynamic 格式**（顶层平铺 `url` + `signature`），而非 Static 的 `platforms` map。
- Rust 必需（缺失即整个响应反序列化失败）：`version`（SemVer，`parse_version` 会 trim 前导 `v`）、`url`（必须能被 `url::Url` 解析）、`signature`（非空）。
- Rust 可选：`notes`（→ `Update.body`）、`pub_date`（**必须 RFC3339 且带时区**，否则整个响应失败）。
- 仅供前端 UI 层 `src/services/updaterModel.ts#parseDesktopUpdateInfo` 使用（走 `rawJson`，Rust 不校验）：`fileName`（回退 URL basename）、`fileSize`（安全正整数）、`isForceUpdate`（严格布尔 `true`）。
- 状态码语义：204 = 无更新（正常），400 = platform 非法，503 = 选中记录元数据不完整。注意 Tauri 对非 2xx 只 log 后 `ReleaseNotFound`，客户端会静默回到 idle，不会崩。
- 响应头必须 `Cache-Control: no-store`；现有 `CORS_HEADERS` 只声明 POST/OPTIONS，需补 GET。

**现状缺口**

- `edge/` 仅实现 license：`index.js` 单入口 → `handleLicenseRequest`（`edge/license-server.mjs`），路由 switch 只有 `POST /api/license/verify|redeem`。
- `src-tauri/tauri.conf.json` 的 `plugins.updater.endpoints` 为空数组；`TAURI_UPDATER_ENDPOINT` 为 GitHub 仓库变量，由 `scripts/write-tauri-release-config.mjs` 写入临时 release config（要求含 `{{target}}` 且 https）。
- `docs/updater-api.md` 头部明确写「本仓库不实现也不部署外部更新服务」——本次要改。
- `desktop-release-manifest.json`（`scripts/build-desktop-release-manifest.mjs`）**不含** `notes` 与 `isForceUpdate`，字段为 schemaVersion/version/tag/generatedAt/repository/commitSha/platforms[{platform, installer, updater{fileName,fileSize,sha256,sourceUrl,signatureFileName,signature}}]；`sourceUrl` 仅在 `PUBLISH_GITHUB_RELEASE=true`（tag 推送）时非空。
- 既有可复用资产：`edge/license-core.mjs`（纯逻辑 + 测试）、`scripts/import-license-cards.mjs` 导出的 `loadSavedEsaCredentials` / `resolveNamespace`、`scripts/clear-kv.mjs` 的 ESA OpenAPI 客户端写法。
- 测试运行方式：`edge/**/*.test.{ts,mjs}` 由 vitest 收集（见 `vitest.config.ts`），**不是** `npm run test:release`（后者只跑 `scripts/*.test.mjs`）。edge 现有 31 个用例通过。

**待用户确认**：发布记录写入方式（KV + 导入脚本 / 边缘函数直连 GitHub / CI 自动写 KV）、notes 与 isForceUpdate 来源。

## 实施完成：edge 新增桌面更新后端（四阶段全部落地）

用户决策：① 发布记录走「导入脚本 + PutKv」；② `notes` / `isForceUpdate` 由导入脚本参数传入；③ 四个阶段一次做完。

**新增文件**

| 文件 | 职责 |
| --- | --- |
| `edge/release-core.mjs` | 纯逻辑：五平台白名单、`release:latest:{platform}` 键、`decideUpdateResponse`（200/204/503）、严格 RFC3339 / SemVer / https URL / 安全正整数校验 |
| `edge/release-core.test.mjs` | 18 个 vitest 用例 |
| `edge/update-server.mjs` | `handleUpdateRequest`：GET 路由、`Cache-Control: no-store`、含 GET 的 CORS、400/405/503/500、`isUpdateRoute` 精确路径判定 |
| `edge/update-server.test.mjs` | 9 个 vitest 用例 |
| `scripts/import-desktop-release.mjs` | 清单校验 → PutKv 写五平台记录；`compareSemver` 降级守卫；GetKv 读现值；限流退避 |
| `scripts/import-desktop-release.test.mjs` | 24 个 node --test 用例（含生产↔消费跨模块契约） |
| `docs/desktop-release-operations.md` | 发布运维手册 |

**改动文件**：`edge/index.js`（精确路径分发，license 逻辑未动）、`package.json`（加 `esa:import-release`）、`scripts/clear-kv.mjs`（补 `release:` 说明）、`docs/updater-api.md`（重写：不再声明「本仓库不实现该服务」，补服务端实现与字段消费位置表）、`docs/license-api.md` + `docs/license-card-operations.md`（补共用命名空间说明）、`README.md`（发布流程第 4 步、更新协议段、`TAURI_UPDATER_ENDPOINT` 示例）。

**关键设计决策**

- 单指针键模型：`release:latest:{platform}` 一条即最新，服务端不排序不比版本（比较在客户端 Tauri 内完成）。
- 导入**先全量校验再整体写入**，任一项不合格整批中止，避免半发布状态。
- 降级守卫默认开启（`--allow-downgrade` 才放行），防止重跑旧 tag 静默回滚全体客户端。
- 清单 `sourceUrl` 非空但非法 → 报错，不静默替换为推导地址。
- 强制更新标记用 `--force-update`（与 `--allow-downgrade` 语义分离）。

**踩坑记录**

- `compareSemver` 必须走 `normalizeVersion` 去 `v` 前缀，否则 `v0.4.0` 与 `0.4.0` 判为不可比较。
- 同一条记录既报「sourceUrl 非法」又报「缺少可用地址」会造成重复告警，改为 `rawSourceUrl || derive(...)` 后按来源二选一报错。
- `pub_date` 校验必须带时区，Rust 侧用 Rfc3339 解析，无时区会拖垮整个响应反序列化。
- `node --test` 跑 `edge/*.test.mjs` 会失败——edge 测试由 vitest 收集（`vitest.config.ts` 的 include），脚本测试才是 `node --test`。

**验证结果**

- `npx vitest run edge/` → 4 文件 58 用例全绿（含原有 license 31 个）。
- `node --test scripts/import-desktop-release.test.mjs` → 24 用例全绿。
- `npm run test:release` → 65 用例全绿。
- CLI 冒烟：dry-run 输出五平台记录正确；坏清单退出码 1、好清单 0、缺文件 1。
- `npm run typecheck` **失败但与本次改动无关**：11 个 `TS2307 Cannot find module '@tauri-apps/api/core'`，位于 `src/services/*.ts` 等未改动文件；`tsconfig.json` 只 include `src/**`，本次新增的 `.mjs` 不在检查范围。疑为 node_modules 由 npm/pnpm 混装（仓库同时存在两份 lockfile）。`npm test` 全量套件未跑完（用户要求跳过）。

